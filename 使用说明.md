# Matheblock Editor 使用说明

> 基于 Unity 的节奏游戏谱面编辑器，围绕「正方体」的 6 个面 × 4 个方向组织 24 条 Note 轨道，支持变 BPM、Note 放置、方体属性缓动编辑与 3D 放映预览。
>
> 当前版本：**0.1.1a**

---

## 目录

- [一、启动与选曲](#一启动与选曲)
- [二、编辑器界面布局](#二编辑器界面布局)
- [三、基础工作流程](#三基础工作流程)
- [四、网格与时间轴](#四网格与时间轴)
- [五、Note 编辑](#五note-编辑)
- [六、方体系统](#六方体系统)
- [七、BPM 管理](#七bpm-管理)
- [八、基本信息面板](#八基本信息面板)
- [九、缓动系统（右侧数据槽）](#九缓动系统右侧数据槽)
- [十、全局事件区](#十全局事件区)
- [十一、音乐控制与放映](#十一音乐控制与放映)
- [十二、保存](#十二保存)
- [十三、设置与快捷键自定义](#十三设置与快捷键自定义)
- [十四、快捷键总表](#十四快捷键总表)
- [十五、谱面文件结构](#十五谱面文件结构)
- [十六、常见问题与提示](#十六常见问题与提示)

---

## 一、启动与选曲

启动程序后进入**选曲（Splash）界面**：

| 操作 | 说明 |
|------|------|
| 选择谱面 | 点击界面中间的「选择谱面」按钮（`ChartButtonClick`），列表从 `Maps/` 目录扫描生成；点击列表项即可进入对应谱面的编辑器 |
| 新建谱面 | 点击「新建谱面」（`NewCharts`），填写曲名、谱师名、曲师名，并通过文件对话框选择音乐文件与封面图片，确认后自动创建谱面目录 |
| 打开设置 | 点击「设置」按钮（`OpenSettings`），以叠加方式打开设置页，无需退出选曲界面 |

> **新建谱面注意事项**：
> - 音乐与封面图片**必须都选择**才能创建成功（支持 mp3/wav/ogg/flac/aac，jpg/png 等）。
> - 音乐文件会复制为 `music.mp3`，封面复制为 `illustration.png`。
> - 若曲名与已有目录重名，会自动追加序号（如 `曲名1`）。

---

## 二、编辑器界面布局

进入编辑场景后，屏幕主要分为以下几个区域：

```
┌──────────────────────────────────────────────────────────┐
│  顶部：音乐时间显示（0.00/总时长）、倍速输入框             │
├──────────────────────────────┬───────────────────────────┤
│  左半：Note 编辑区（网格）     │  右半：缓动数据槽区         │
│  · 垂直线 = 轨道（XLine）     │  · 15 条竖线 = 15 个属性槽  │
│  · 水平线 = 时间轴（YLine）   │  · 长条 + 缓动曲线可视化    │
│  · 橙色标定线（基准线）       │                           │
├──────────────────────────────┴───────────────────────────┤
│  底部/左上：播放控制（Play / Pause / Replay / Display）    │
│  UpperList：方体 ID 输入 + 面（6）+ 方向（4）按钮           │
│  FunctionChanger：BPM 管理 / 基本信息 / 方体管理 等按钮     │
│  Save 按钮：保存谱面                                        │
└──────────────────────────────────────────────────────────┘
```

- **左半区域**（`m_noteAreaRatio = 0.5`）：放置 Note 的网格。
- **右半区域**：方体 15 个属性的缓动数据槽（详见[第九节](#九缓动系统右侧数据槽)）。
- 中央是 3D 方体的实时预览（CubeCamera 渲染），背景为预模糊的曲绘。
- 预览区竖中线顶部显示 **Combo 计数**（数字 + "COMBO" 标签，详见[11.4](#114-combo-计数与背景)）。
- 编辑模式下背景曲绘与方体展示区会整体**变暗**（0.4 亮度），突出编辑内容（详见[11.4](#114-combo-计数与背景)）。

---

## 三、基础工作流程

1. 在选曲界面选择或新建谱面。
2. 在 **BPM 管理**面板设置变 BPM 节点（时间 + BPM）。
3. 在 **基本信息**面板填写歌曲名、谱师名、曲师名、offset 等。
4. 通过 **UpperList** 选择当前编辑的方体、面、方向（24 条轨道之一）。
5. 在左半网格中**放置 Note**，在右半缓动区**编辑方体动画**。
6. 点击 **Save** 保存（或依赖自动保存 / 退出时自动保存）。

---

## 四、网格与时间轴

### 4.1 网格线

| 线条 | 含义 |
|------|------|
| 垂直线 | 轨道，数量由 `XLine` 输入框控制（默认 4 条），分布在左半 Note 区 |
| 水平线 | 时间轴，密度由 `YLine` 输入框控制；按 BPM 节点动态变化间距 |
| 金色粗线 | 整拍（Beat）线 |
| 紫色竖线 | 左右区域分隔线 |
| 橙色粗线 | **标定线**（固定在视口 3/4 处），用于对准节拍 |

### 4.2 滚动与缩放

| 操作 | 效果 |
|------|------|
| 鼠标滚轮 | 垂直滚动时间轴 |
| `Ctrl` + 滚轮 | 缩放（0.1x ~ 8x，步进 1.1 倍） |
| 方向键 ↑ / ↓ | 上下滚动（`Editor_ScrollUp` / `Editor_ScrollDown` 快捷键） |
| `Ctrl` + `=` / `-` | 键盘缩放 |

> 缩放只改变视觉密度，**节拍位置不会改变**。

### 4.3 时间滑块

拖动底部/顶部的 `MusicTime` 滑块可快速跳转到任意时间点，时间显示会同步更新。

---

## 五、Note 编辑

### 5.1 Note 类型

| Note 类型 | 快捷键（默认） | 说明 |
|-----------|----------------|------|
| Click | `Q` | 单击型 Note |
| Flick | `R` | 滑动型 Note |
| Drag | `E` | 拖拽型 Note |
| ReverseFlick | `T` | 反向 Flick（图片 Y 轴翻转） |
| Hold | `W` | 长按型 Note，**按两次 W 确认**（见下文） |

### 5.2 放置方式

1. 将鼠标移动到左半 Note 区，悬停位置会显示**半透明吸附指示器**（自动吸附到最近的节拍格点）。
2. 按下对应类型的快捷键，即在悬停格点放置 Note。
3. 同一格点已有 Note 时会自动跳过（去重）。

**Hold 放置**：
- 第一次按 `W`：生成 Hold 头部，进入待确认状态。
- 第二次按 `W`（**同一列**）：自动填充中间与尾部，完成 Hold。
- 按 `Esc` 取消待确认状态；移动到不同列再按 `W` 会重新开始。

### 5.3 选择

| 操作 | 效果 |
|------|------|
| 左键单击 Note | 选中（单选） |
| `Ctrl` + 单击 | 切换选中 |
| `Shift` + 单击 | 范围选择（按时间选取锚点之间的所有 Note） |
| 空白处拖拽 | **框选**（拖出半透明矩形框） |
| `Ctrl` + 拖拽框选 | 追加式框选 |
| `Ctrl` + `A` | 全选 |
| `Esc` | 取消所有选择 |

### 5.4 移动与删除

| 操作 | 效果 |
|------|------|
| 选中 Note 后拖拽 | **集体移动**（自动吸附节拍，且不越界） |
| `Delete` | 有选中时批量删除；无选中时删除悬停位置的 Note |
| `Ctrl` + `Z` / `Ctrl` + `Y` | 撤回 / 重做（含放置、删除、移动、BPM、Info、缓动长条） |

> 选中 Note 显示为**天蓝色高亮**。

### 5.5 Fake Note（假音符）

Fake Note 与普通 Note 的唯一区别是**击打后不生成打击特效**，编辑器内以**半透明（alpha 0.5）**显示，适合制作迷惑性谱面段落。

放置模式可在设置中切换（默认**切换**）：

| 模式 | 操作 |
|------|------|
| 切换（默认） | 按一次 `Tab` 开启 Fake 模式（悬停指示器变红），再按一次关闭 |
| 按住 | 按住 `Tab` 的同时按对应类型快捷键 |

Fake 模式下放置的 Note 均为 fake note，可与任意类型快捷键组合（`Fake + Q/R/E/T/W`）。

---

## 六、方体系统

谱面的核心概念是「正方体」，所有 Note 轨道围绕 **6 个面 × 4 个方向 = 24 条轨道** 组织：

| 概念 | 数量 | 说明 |
|------|------|------|
| 面（CubeFace） | 6 | 上 / 下 / 左 / 右 / 前 / 后 |
| 方向（FaceDirection） | 4 | 每个面的上 / 下 / 左 / 右 |
| Note 轨道 | 24 | 每条轨道独立存储 Note 与轨道级缓动数据 |

### 6.1 UpperList 快捷选择

- **CubeID 输入框**：输入方体 ID 后回车，切换到对应方体。
- **Surface 按钮组**：6 个面按钮（上/下/左/右/前/后），选中项高亮为黄色。
- **Side 按钮组**：4 个方向按钮（上/下/左/右）。

切换面/方向时，当前轨道的 Note 会自动保存到旧轨道，并加载新轨道的 Note。

### 6.2 方体管理面板

点击 FunctionChanger 下的「CubeManager」按钮打开全屏面板：

| 操作 | 说明 |
|------|------|
| `+ 创建方体` | 创建新方体（ID 从 2 开始递增），自动初始化 24 条空轨道 |
| 选中 | 切换当前编辑的方体 |
| `X` 删除 | 删除方体（**仅剩 1 个时禁止删除**） |
| 备注输入框 | 编辑方体备注，回车保存 |

> 首次打开谱面时若无方体数据，会自动创建默认方体 `Cube_1`。

---

## 七、BPM 管理

点击 FunctionChanger 下的「BPM管理」按钮打开面板：

| 操作 | 说明 |
|------|------|
| `+ 添加节点` | 以当前音乐时间（滑块位置）添加新 BPM 节点，BPM 自动继承上一个节点 |
| 时间输入框 | 编辑节点时间（秒），需**升序且不重复**，违规会自动回退并弹出提示 |
| BPM 输入框 | 编辑该节点起的 BPM 值 |
| `X` 删除 | 删除节点（**第一个节点不可删除**） |
| 左上角 `<` | 返回编辑界面 |

- BPM 节点按时间升序排列，修改后网格水平线立即刷新。
- 谱面在任意时间点的 BPM = 最近一个「时间 ≤ 该时间」的节点 BPM。

---

## 八、基本信息面板

点击 FunctionChanger 下的「基本信息」按钮打开面板：

| 字段 | 说明 |
|------|------|
| 歌曲名 / 谱师名 / 曲师名 / 曲绘作者 | 谱面元信息，显示在选曲列表 |
| offset | **音乐偏移（毫秒）**：音乐快了 offset 毫秒时，播放会将其延后以对齐谱面 |
| 曲绘预览 | 显示当前曲绘 |
| 更换曲绘 | 打开文件对话框选择新图片，自动复制为 `illustration.png` 并刷新背景 |

> 编辑内容会自动保存到 `chart.tmp`，点 Save 后写入正式文件。

---

## 九、缓动系统（右侧数据槽）

右侧区域为方体的 **15 个属性**提供「数值随时间变化」的可视化关键帧编辑。

### 9.1 15 个数据槽

| 槽索引 | 标号 | 含义 | 默认值 | 范围 |
|--------|------|------|--------|------|
| 0-2 | lx / ly / lz | 方体长宽高（百分比，100 = 原始大小） | 100 | 0 ~ 200 |
| 3-5 | rx / ry / rz | 方体倾斜角（度） | 0 | -360 ~ 360 |
| 6-8 | px / py / pz | 方体位置 | 0 | -10 ~ 10 |
| 9 | R | 颜色 R | 0.9 | 0 ~ 1 |
| 10 | G | 颜色 G | 0.9 | 0 ~ 1 |
| 11 | B | 颜色 B | 0.9 | 0 ~ 1 |
| 12 | A | 颜色 A（可 >1 增强不透明度） | 1 | 0 ~ 2 |
| 13 | 棱偏移 | Note 相对中间位置的偏移 | 0 | -1 ~ 1 |
| 14 | 流速 | Note 下落速度倍率 | 30 | 0 ~ 60 |

> 槽 0-12 为**方体级**属性（作用于整个方体），槽 13-14 为**轨道级**属性（作用于当前选中的面+方向轨道，显示为橙色）。

### 9.2 长条（关键帧区间）

- **创建长条**：鼠标悬停在某个数据槽内，按 `S` 键**两次确认**——
  - 第一次按 `S` 确定起点（出现紫色标记），移动鼠标后第二次按 `S` 确定终点。
  - 起点与终点在**同一格点** → 创建**瞬时赋值事件**（浅绿色，表示在该时刻瞬间赋值的数值）。
  - 时长不足半拍会被拒绝。
  - 按 `Esc` 可取消待定状态。
- **选中长条**：点击长条，选中后自动弹出**长条编辑面板**。
- **删除长条**：选中后按 `Delete`，或在编辑面板点「删除长条」。
- **长条之外**：数值保持前一个长条的结束值（或默认值），不随时间变化。

### 9.3 长条编辑面板

| 项 | 说明 |
|----|------|
| 事件类型 | 显示「瞬时赋值事件」（绿色）或「数值变化事件」（紫色） |
| 方体 / 面 / 方向 | 当前长条所属对象 |
| 时间 | 长条的起始 ~ 结束时间 |
| 起始值 / 结束值 | 修改长条头尾数值（瞬时事件会自动同步两端） |
| 缓动 | 下拉选择缓动类型（31 种 DOTween Ease） |
| 权重 | 滑块 0 ~ 2（0 = 线性，1 = 完整缓动，>1 = 增强缓动） |
| 曲线预览 | 实时绘制当前缓动曲线（对角参考线 = 线性） |
| 删除长条 / 关闭 | 删除或取消选中 |

> `time = 0` 处的初始瞬时事件（各槽默认值）**不可删除**。

### 9.4 水平滚动

- 在缓动区**拖拽鼠标**可水平滚动查看所有数据槽。

---

## 十、全局事件区

按 `O` 键（`Global_Toggle`）切换**全局事件区模式**，将谱面内**所有方体的所有缓动事件**按时间汇总展示在右侧：

- 每个事件占用一条水平轨道（自动分配，避免时间重叠）。
- 长条上标注**数据槽代号**（左下）与**来源方体**（右下，轨道级事件额外显示面+方向）。
- 事件颜色：蓝紫 = 方体级数值事件，青色 = 瞬时事件，橙色 = 轨道级事件。
- `time=0` 且值为默认值的初始瞬时事件不显示。

**全局模式下的编辑**：

| 操作 | 说明 |
|------|------|
| `S` 两次 | 创建全局长条（继承当前选中事件的方体/槽/轨道；无选中时使用活跃方体槽 0） |
| `Delete` | 删除选中的全局长条 |
| 点击长条 | 选中并弹出编辑面板（增加「全局模式设置」区域） |
| 编辑面板中的方体 ID / 面 / 方向 | 将选中事件迁移到其他方体或轨道 |

再次按 `O` 退出全局事件区，恢复普通模式。

---

## 十一、音乐控制与放映

### 11.1 播放控制

| 按钮 | 说明 |
|------|------|
| Play | 从当前滑块位置开始播放 |
| Pause | 暂停 |
| Replay | 回到开头并**放映**（淡出网格/Note/缓动区，进入 3D 放映模式） |
| Display | **放映但不隐藏网格**（网格保持可见并跟随播放自动滚动） |

### 11.2 倍速

在顶部「Speed」输入框中输入倍速（0.05 ~ 10），回车应用。用于慢速校对或快速预览。

### 11.3 放映模式

- 播放时自动进入**放映模式**：网格 / Note / 缓动区 / 标定线淡出，3D Note 从相机视野外下落，命中棱外边缘时销毁并播放打击特效。
- 暂停或停止后自动恢复编辑界面。

### 11.4 Combo 计数与背景

**Combo 计数**：预览区顶部实时显示已命中的 Note 数（第几个键消失 Combo 就是几，Fake Note 不计入，Hold 只在击打开头计一次）。由于是既定播放器、默认每个键都被精准击中，Combo **完全由当前时间决定**——拖动时间滑块跳转到任意位置，Combo 会立即同步为该时间点已消失的 Note 数量，不会因中途暂停/继续或跳转而清零。

**背景变暗**：编辑（预处理）模式下背景曲绘与方体展示区均变暗至 0.4 亮度，突出编辑内容。放映时曲绘保持变暗（0.4），方体展示区恢复全亮；Display 放映时两者均保持变暗。

| 区域 | 编辑模式 | 放映（Play） | Display 放映 |
|------|----------|--------------|--------------|
| 背景曲绘 | 0.4 暗 | 0.4 暗 | 0.4 暗 |
| 方体展示区 | 0.4 暗 | 1.0 亮 | 0.4 暗 |

---

## 十二、保存

### 12.1 保存机制（双文件工作流）

| 文件 | 作用 |
|------|------|
| `chart.json` | 正式谱面文件 |
| `chart.tmp` | 编辑期工作副本（启动时由 `chart.json` 复制生成，退出时清理） |

- 所有编辑操作（Note / BPM / Info / 方体 / 缓动）即时写入 `chart.tmp`。
- 点击 **Save 按钮**时，将 `chart.tmp` 覆盖写入 `chart.json` 完成持久化。
- **退出程序时自动保存**（先持久化再清理临时文件）。

### 12.2 自动保存

在设置中可配置自动保存间隔（分钟，0 = 关闭），到时间后自动持久化到 `chart.json`。

> 建议编辑过程中偶尔手动点 Save，或保持自动保存开启。

---

## 十三、设置与快捷键自定义

点击选曲界面的「设置」或编辑器内按 `Ctrl + E`（Editor 模式）打开设置页（Additive 叠加），分为左侧菜单多个子页面：

### 13.1 常规设置

| 设置项 | 说明 |
|--------|------|
| 主音量 / 音乐音量 / 音效音量 | 0 ~ 1 |
| 分辨率 | 下拉选择 |
| 画质 | 固定为高画质（High），不提供配置项 |
| 自动保存间隔 | 分钟数，0 = 关闭 |
| Fake Note 模式 | 切换（默认）/ 按住 |

设置保存在 `persistentDataPath/Settings.json`，首次运行自动从旧版 PlayerPrefs 迁移。

### 13.2 快捷键重绑

在「快捷键」页面（`Page_ShortcutKey`），点击任意行的 **RebindButton**，5 秒内按下新按键即可绑定：

- 支持 `Ctrl` / `Shift` / `Alt` 组合键及鼠标滚轮上/下。
- 按 `Esc` 视为取消（不保存）。
- 可重绑项见[快捷键总表](#十四快捷键总表)中的「可自定义」列。

绑定保存在 `persistentDataPath/KeyBindings.json`，关闭设置页后自动生效。

---

## 十四、快捷键总表

| 快捷键（默认） | 功能 | 可自定义 |
|----------------|------|----------|
| `Ctrl + Z` | 撤回 | ✅ `Editor_Undo` |
| `Ctrl + Y` | 重做 | ✅ `Editor_Redo` |
| `Ctrl + Shift + Z` | 重做（兼容） | 自动兼容 |
| `Q` | 放置 Click Note | ✅ `Note_Click` |
| `R` | 放置 Flick Note | ✅ `Note_Flick` |
| `E` | 放置 Drag Note | ✅ `Note_Drag` |
| `T` | 放置 ReverseFlick Note | ✅ `Note_ReverseFlick` |
| `W` | 放置 Hold Note（两次确认） | ✅ `Note_Hold` |
| `Delete` | 删除（选中批量 / 悬停单个） | ✅ `Note_Delete` |
| `Tab`（切换/按住） | Fake Note 切换键 | ✅ `Note_FakeToggle` |
| `O` | 切换全局事件区模式 | ✅ `Global_Toggle` |
| `S` | 创建缓动区长条（两次确认） | ✅ `Bar_Create` |
| `Delete` | 删除选中长条（缓动区） | ✅ `Bar_Delete` |
| 滚轮上 / 滚轮下 | 向上 / 向下滚动 | ✅ `Editor_ScrollUp/Down` |
| `Ctrl` + 滚轮上 / 下 | 放大 / 缩小 | ✅ `Editor_ZoomIn/ZoomOut` |
| `Ctrl + E` | 打开设置（编辑器内） | ❌ |
| `Esc` | 关闭设置 / 取消选择 / 取消长条待定状态 | ❌ |

> 组合键格式：`Ctrl + Shift + Alt + 主键`，主键为修饰键时也支持单独绑定。

---

## 十五、谱面文件结构

每个谱面是 `Maps/` 下的一个目录：

```
Maps/<谱面名>/
├── chart.json         # 正式谱面文件（info / bpmNodes / notes / cubes）
├── chart.tmp          # 编辑期临时副本（运行时生成，退出清理）
├── music.mp3          # 音乐文件
└── illustration.png   # 曲绘（封面）
```

`chart.json` 核心结构：

```json
{
  "info": {
    "MusicName": "Chronomia",
    "Charter": "unifoly",
    "Illustrationer": "",
    "Musician": "Lime",
    "offset": 0
  },
  "bpmNodes": [
    { "time": 0.0, "bpm": 120.0 },
    { "time": 3.0, "bpm": 30.0 }
  ],
  "notes": [
    { "type": "Click", "lane": 0, "time": 1.0, "endTime": 0, "isFake": false }
  ],
  "cubes": [
    {
      "cubeId": 1,
      "cubeName": "Cube_1",
      "cubeNote": "",
      "tracks": [
        { "face": "Up", "direction": "Up", "notes": [], "easingSlots": [] }
      ],
      "easingSlots": []
    }
  ]
}
```

---

## 十六、常见问题与提示

1. **Note 放不上？** 确认鼠标位于左半 Note 区且出现了半透明指示器；同格点已有 Note 时会自动跳过。
2. **Hold 放不出来？** 需在同一条轨道（列）上按两次 `W`；起止点相同会自动取消；按 `Esc` 可放弃。
3. **缓动区长条创建失败？** 两次按 `S` 需在同一数据槽；时长需 ≥ 半拍；`time=0` 已存在初始事件，请选择其他时间点。
4. **无法删除长条？** `time=0` 的初始瞬时事件不可删除；其余需先选中（点击）再按 `Delete`。
5. **切面/切方向后 Note 不见了？** 24 条轨道独立存储，切换后会加载新轨道的 Note；切回原面+方向即可找回。
6. **Fake Note 想改回普通？** 删除后重新放置，或关闭 Fake 模式后再放。
7. **修改 offset 不生效？** offset 单位为**毫秒**，音乐快了 offset 毫秒时播放会将其延后以对齐谱面；保存后重新播放生效。
8. **找不到谱面？** 谱面目录需放在程序目录下的 `Maps/` 文件夹中，且必须包含 `chart.json`（缺少会从列表跳过并在日志中警告）。
9. **担心数据丢失？** 编辑内容即时写入 `chart.tmp`，退出时自动持久化到 `chart.json`；建议保持自动保存开启并定期手动点 Save。

---

> 更深入的技术架构与开发说明见 [TECHNICAL_DOCUMENT.md](TECHNICAL_DOCUMENT.md)。
